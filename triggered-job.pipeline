pipeline {
  agent any
    triggers {
        when {
            cron params.cron
        }
            
    }
  stages {
    
    stage('Hello World') {
      steps {
        echo 'Hello World'
        echo "${params.cronsetup}"
      }
    }
  }
